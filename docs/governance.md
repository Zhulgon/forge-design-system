# Governance

FORGE follows semantic versioning.

- PATCH — bug fix, docs or accessibility/visual correction without public API change.
- MINOR — additive backward-compatible component, property or token.
- MAJOR — rename/removal, changed state meaning, token contract break or required migration.

A domain primitive may enter Core only after proven reuse in at least two independent verticals and a semantic/accessibility review.
